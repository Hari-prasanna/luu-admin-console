# Oracle + alerting credentials — copy to oracle.env and fill in real values

ORA_USER=your_readonly_user
ORA_HOST=your.oracle.host
ORA_PORT=1521
ORA_SERVICE=your_service_name
ORA_PASSWORD=your_password_here

# Optional — Google Chat webhook for metric state-change alerts
CHAT_WEBHOOK_URL=
